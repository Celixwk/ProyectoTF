providers = ["node"]

[phases.install]
cmds = ["cd backend && npm install", "cd backend && npx prisma generate"]

[phases.build]
cmds = [
    "cd backend && npx tsc",
    "mkdir -p backend/dist/generated backend/dist/database",
    "cp -r backend/src/generated/* backend/dist/generated/",
    "cp -r backend/src/database/* backend/dist/database/"
]

[start]
cmd = "cd backend && npm start"
