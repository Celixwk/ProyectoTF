providers = ["node"]

[phases.install]
cmds = [
    "cd backend && npm install",
    "cd backend && npm run prisma:generate",
    "cd backend && npx prisma db push --accept-data-loss",
    "cd backend && node src/scripts/setup-database.js"
]

[start]
cmd = "cd backend && npm start"
